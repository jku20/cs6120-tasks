@main {
  one: int = const 1;
  another_one: int = const 1;
  yet_another: int = add one one;
  still_redundant: int = add one one;
  print one;
  print another_one;
  print yet_another;
  print still_redundant;
}
